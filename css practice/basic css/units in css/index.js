// Length in CSS.

// 1. Css has several unit of length.
// 2. width, height, font.

// 1. Absolute: It is prevalent and of that specific size that we have given.

/*
    Not recommended for the usage on multiple screen.

    px -> pixel
    cm -> centimeter
    mm -> millimeter
    pt -> pointes
*/

// 2. Relative: Length of an object Relative to a Parent Object.

/*
    2.1 Font related sizes -> em ,rem, etc.
    2.2 ViewPort -> vh, vw, etc.. (vh -> view height, vw -> view width)
    2.3 Parent Relative Length -> %
*/

// What is "outline" used for?
